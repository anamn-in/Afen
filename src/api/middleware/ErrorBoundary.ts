// Logging and error handling now managed by Fastify's built-in logger
// These stubs remain for backward compatibility during migration.

export function requestLogger() {}
export function errorBoundary() {}